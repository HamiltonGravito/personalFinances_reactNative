# APP Personal Finances (React Native)

### Criação do App (EXPO).
`npx create-expo-app app_Name` ou `npx create-expo-app .` -> (representa o diretório corrent)

### Executar App.
`npx expo start`

## Criar Navegações
